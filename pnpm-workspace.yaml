packages:
  - "packages/*"
  - "apps/*"
  - "extensions/*"
  # apps/workbench included via apps/*

# pnpm 11: allow lifecycle scripts for native bundler
allowBuilds:
  esbuild: true

onlyBuiltDependencies:
  - esbuild
