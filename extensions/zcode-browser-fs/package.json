{
  "name": "zcode-browser-fs",
  "displayName": "ZCode Browser FS",
  "description": "Shared IndexedDB FileSystemProvider (zcode-opfs) for SPA + VS Code Web",
  "version": "0.2.0",
  "publisher": "spinupdev",
  "private": true,
  "engines": {
    "vscode": "^1.85.0"
  },
  "categories": ["Other"],
  "extensionKind": ["ui", "workspace"],
  "browser": "./dist/web/extension.js",
  "activationEvents": [
    "onStartupFinished",
    "onFileSystem:zcode-opfs",
    "onCommand:zcode.fs.openWorkspace",
    "onCommand:zcode.fs.seedSample",
    "onCommand:zcode.fs.openSpa"
  ],
  "contributes": {
    "commands": [
      {
        "command": "zcode.fs.openWorkspace",
        "title": "ZCode: Open Virtual Workspace"
      },
      {
        "command": "zcode.fs.seedSample",
        "title": "ZCode: Seed Sample Files"
      },
      {
        "command": "zcode.fs.openSpa",
        "title": "ZCode: Open Browser Git SPA"
      }
    ]
  },
  "scripts": {
    "build": "node ./scripts/build.mjs",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "node -e \"process.exit(0)\"",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@zcode/browser-agent": "workspace:*"
  },
  "devDependencies": {
    "@types/vscode": "^1.95.0",
    "esbuild": "^0.25.5",
    "typescript": "^5.8.3"
  }
}
