# zcode-runtime-node

Run **JavaScript / TypeScript** in a **browser Worker** (WB2 v1).

- Backend id: `browser-node`
- **Not** full Node.js (no `require`, `fs`, npm). Pure JS console.log demos work.
- Future: WebContainers / WASI as drop-in replacement behind the same registry id.
